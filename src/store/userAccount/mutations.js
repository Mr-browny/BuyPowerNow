 
export function load_user (state, payload) {
    // state.user = {} /* This empties the state first then re-assign it, flexible when using the mapActions load_user in  update profile. */
    // state.user = payload 
} 
